package com.proyecto.taskmanager.usuario.enums;

/**
 * Roles de la aplicación.
 * <p>Se mapean a la columna {@code usuarios.rol} que tiene CHECK ('ADMIN','USER').</p>
 */
public enum Rol {
    ADMIN, USER
}
